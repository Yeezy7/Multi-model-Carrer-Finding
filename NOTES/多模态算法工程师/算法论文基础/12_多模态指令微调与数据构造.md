# 多模态指令微调与数据构造：SFT、LoRA 与指令数据的完整剖析

> 本笔记是面试"怎么把你的 VLM 从'会看图说话'变成'会执行指令'"这一问题的完整答案。核心内容三块：**为什么微调**（对齐任务与能力转变）、**怎么微调**（SFT 原理 + LoRA/QLoRA 策略 + 多模态特有考量）、**拿什么微调**（指令数据构造）。纯理论，不涉及任何具体代码仓库。

## 一、为什么需要微调：从"看图说话"到"遵循指令"

### 1.1 预训练模型的两层能力错位

VLM 的预训练（对齐阶段，如 LLaVA 的 Stage-1、Qwen2-VL 的预训练）做的事情是：把视觉塔输出的 patch 特征通过投影层映射进 LLM 的词嵌入空间，然后用"图像描述/图文对"这类数据训练，目标是**让 LLM 能理解图像 token 的含义、能把图像内容转换成语言**。

但预训练结束时的模型只具备一个能力：**把图像概括成一段文字（caption）**。它并不具备以下任何能力：

| 预训练后缺失的能力 | 具体表现 |
|-------------------|---------|
| 遵循指令 | 问"图里有几只猫"会得到一段环境描述，而不是"3 只" |
| 对话格式 | 不会按 system/user/assistant 轮次组织输出 |
| 任务行为 | 不会"只回答、不解释"；不会拒绝回答；不会输出 JSON |
| 细粒度感知 | 只给全局描述，无法定位、计数、读 OCR |
| 交互能力 | 多轮对话中不会结合上文继续回答 |

**一句话总结：预训练学的是"语义映射"，微调学的是"行为规范"。**

### 1.2 SFT（Supervised Fine-Tuning）的定义

SFT = 用**人工标注的（指令，回答）配对数据**对模型进行有监督训练，让模型学会"给定一个指令，输出符合预期的回答"。

类比：预训练是"读书"，让模型有知识储备；SFT 是"军训"，教模型答题格式和答题规范。知识在预训练里已经学完了，**SFT 不负责增加知识，只负责改变输出行为**——这是面试中最容易被追问的点：*"SFT 之后模型知识变多了吗？"答案：几乎不变，变的只是输出的条件分布 $P(\text{answer} \mid \text{image}, \text{instruction})$ 的形状。*

### 1.3 微调的两阶段范式（以 LLaVA 为代表）

| 阶段 | 训练数据 | 训练目标 | 训练模块 |
|------|---------|---------|---------|
| Stage-1 特征对齐 | 图文对（CC3M 等） | 视觉特征 → 词嵌入空间 | 投影层（冻结 ViT + LLM） |
| Stage-2 指令微调 | 指令数据（LLaVA-158K 等） | 遵循指令生成回答 | 投影层 + LLM（冻结 ViT） |

注意一个关键区别：**对齐（alignment）≠ 指令遵循（instruction following）**。Stage-1 让模型"看得懂图"，Stage-2 让模型"听得懂指令"。

---

## 二、微调策略全景表

### 2.1 五种策略对比

| 策略 | 训练的参数 | 可训练参数量(7B 级) | 显存(近似) | 效果 | 适用场景 |
|------|-----------|-------------------|-----------|------|---------|
| 全参微调 (Full FT) | 所有参数 | 100% (~7B) | 约 70–100 GB（需多卡） | 上限最高 | 有算力、基座可充分获取时 |
| 冻结视觉塔+全参训 LLM | ViT 冻结，LLM+投影层全训 | ~90% | 约 60–90 GB | 高 | LLaVA Stage-2 标准做法 |
| 只训投影层 | 仅 MLP projector | ~0.5–1% | 约 20–40 GB | 中低（只对齐不改变行为） | Stage-1 对齐、轻量适配 |
| LoRA | 低秩增量（q/k/v/o+MLP） | ~0.1–1% | 约 16–30 GB（单卡可跑） | 接近全参微调 | 绝大多数实际场景 |
| QLoRA | 4-bit 量化基座 + LoRA | ~0.1–1% + 量化基座 | 约 8–16 GB（消费级单卡） | 略低于 LoRA | 单卡 24G 内训练 7B/13B |

### 2.2 为什么 LoRA 成为默认选择

- 7B 模型全参微调，AdamW 需要保存**一份参数 + 两份优化器状态（一阶矩、二阶矩）+ 梯度**，仅优化器状态就是 $2 \times 4 \text{ bytes} \times 7B \approx 56$ GB，还没算参数和激活；
- LoRA 只更新少量低秩矩阵，**优化器状态和梯度只针对可训练参数**，显存大头变成"前向推理 + 激活"，7B 模型 24 GB 单卡即可训练；
- 效果上，已有大量实验（QLoRA 论文、LoRA 论文及后续实证）表明：**在指令微调规模下（1–3 epoch 小数据量），LoRA 与全参微调的效果差距很小**，尤其在 VLM 微调场景，微调本质上只是"行为迁移"而非"知识学习"，低秩更新足够表达。

---

## 三、SFT 原理（重点）

### 3.1 目标函数：只对回答部分算交叉熵

SFT 的损失函数与预训练完全相同——**标准语言模型交叉熵（next-token prediction）**。唯一的区别是**损失掩码（label mask）**：

$$\mathcal{L}_{\text{SFT}} = -\sum_{t} \mathbb{1}[t \in \text{answer}] \log P_\theta(x_t \mid x_{<t}, I)$$

其中：

- $I$：输入图像（经视觉塔 + 投影层变成视觉 token 拼进序列）；
- $x_t$：第 $t$ 个 token；
- $\mathbb{1}[t \in \text{answer}]$：**指示函数，只有当 $t$ 落在 assistant 回答部分时才计算 loss**；
- prompt 部分（system、user、图像描述指令）的 token 只参与条件计算（作为上下文），不参与梯度回传。

等价写法：对完整序列的交叉熵损失乘上一个掩码向量 $\mathbf{m} \in \{0,1\}^T$：

$$\mathcal{L}_{\text{SFT}} = -\frac{1}{\sum_t m_t} \sum_{t=1}^{T} m_t \log P_\theta(x_t \mid x_{<t})$$

**分母是有效 token 数（answer 部分的 token 数），不是序列全长**——这是 SFT 与预训练在实现上的核心差异。

### 3.2 为什么只对回答部分算 loss（三个理由）

| 理由 | 解释 |
|------|------|
| 防止记忆问题 | 若对 prompt 也算 loss，模型会"背"输入格式与问题本身，削弱对回答的专注；且会让模型学会预测"下一个问题"这种无意义能力 |
| 提高训练效率 | prompt 通常比回答长（尤其多轮对话），掩码后有效梯度只来自回答部分，训练信号更密集 |
| 避免分布污染 | 若让模型学习"复述指令"，会污染后续 RLHF/DPO 阶段"模型应该对指令敏感"的行为边界 |

> **面试记忆点**：SFT 的交叉熵与预训练唯一不同是 **mask 的位置**——只在 assistant 回答 token 上回传梯度。这直接对应实现里的 `labels` 被设置为 -100 的地方（-100 是 PyTorch 交叉熵的 ignore index，与理论上的 $m_t=0$ 等价）。

### 3.3 训练数据格式：conversation 格式

SFT 数据以多轮对话组织，结构为若干轮 `(role, content)` 序列，典型角色：

| 角色 | 含义 | 示例 |
|------|------|------|
| `system` | 全局指令/人设（可选） | "你是一个细心的多模态助手" |
| `user` | 人类输入（可含图像占位符） | `<image> 这张图里有什么危险？` |
| `assistant` | 模型应输出的回答（**loss 只算这里**） | "图中有一个人在高压线下放风筝，存在触电风险。" |

在 VLM 中，图像通常以特殊 token（如 `<image>`）嵌入 user 轮次中，训练时由 processor 替换为真实的视觉 token 序列。多轮对话格式示例（理论示意）：

```text
[system] 你是一个专业的图表分析助手。
[user] <image> 帮我总结这张图的趋势。
[assistant] 该图展示的是……（仅此处参与 loss 计算）
[user] 那拐点出现在哪里？
[assistant] 拐点出现在第 30 个月，此时……（仅此处参与 loss 计算）
```

### 3.4 HF ChatTemplate 与 tokenizer 的角色

HuggingFace 的对话模板（chat template）负责把上面的结构化对话**渲染成一段纯文本序列**，再交给 tokenizer 编码：

```text
<|im_start|>system\n你是一个图表分析助手。<|im_end|>\n
<|im_start|>user\n<image> 帮我总结趋势。<|im_end|>\n
<|im_start|>assistant\n该图展示的是……（本段及后续 assistant 段是 loss 区间）
```

两个面试高频细节：

1. **模板必须与基座一致**：不同基座（LLaMA、Qwen、Mistral）的模板不同（如 Qwen 用 `<|im_start|>`，LLaMA 3 用 `<|start_header_id|>`）。模板不一致会导致训练与推理时 token 分布偏移，模型输出崩坏；
2. **labels 的构造顺序**：先渲染完整文本 → 逐段编码 → 在拼接处将 user/system 部分对应的 labels 置为 -100，只保留 assistant 段。实现上常借助 `apply_chat_template` 的 `tokenize` 参数或对每段分别编码后拼接。

### 3.5 Epoch 设置：为什么 SFT 通常只有 1–3 个 epoch

| 现象 | 原因 |
|------|------|
| SFT 数据量小（万～百万级条） | 与预训练的万亿 token 相比是零头，模型很快见过全部数据 |
| 容易过拟合 | 数据少 + 行为迁移性质 → 过多 epoch 会让模型死记训练模板与措辞，泛化下降 |
| 通常 1–3 epoch | LLaVA、ShareGPT4V、Qwen-VL 等主流工作均采用 1 个 epoch 或微调少量步数 |
| 过拟合信号 | 验证集 loss 回升但训练 loss 逼近 0；测试时输出"套模板"感明显 |

实操经验：**指令微调一般 1 epoch 足够**（LLaVA-1.5 只用 1 epoch 即可取得 SOTA 水平）；若数据量极小（几千条），可 3 epoch 并配轻量正则（dropout、LoRA 下配权重衰减）。判断标准永远看验证集，而不是训练集 loss。

---

## 四、LoRA（重点）

### 4.1 核心思想：低秩更新假设

全参微调时，每个权重矩阵 $W_0 \in \mathbb{R}^{d \times k}$ 都会有一个增量 $\Delta W$。LoRA 的核心假设是：

> **微调导致的权重变化 $\Delta W$ 是低秩的**——即 $\text{rank}(\Delta W) \ll \min(d, k)$，可以用两个小矩阵的乘积近似。

直观理由：

- 微调是在一个已收敛的强预训练点上做"小幅行为调整"，需要的"自由度"远小于重新学习整个表示；
- 实验证据：对训练好的 $\Delta W$ 做 SVD 分解，只保留前 $r$ 个奇异值（$r=1$ 或 $r=2$ 甚至 $r=4$ 就占了绝大部分能量），效果几乎不下降（LoRA 论文 4.2 节实验）。

### 4.2 数学形式

不直接更新 $W_0$，而是冻结 $W_0$，引入低秩增量：

$$h = W_0 x + \Delta W x = W_0 x + \frac{\alpha}{r} B A x$$

其中：

- $A \in \mathbb{R}^{r \times k}$：随机高斯初始化；
- $B \in \mathbb{R}^{d \times r}$：零初始化（保证训练开始时 $\Delta W = 0$，模型输出与基座完全一致，训练起点稳定）；
- $r$：秩（rank），远小于 $d, k$；
- $\alpha$：缩放系数，LoRA 论文按 $\alpha = 2r$ 的经验值设置（新实现常把 $\frac{\alpha}{r}$ 直接作为整体缩放并入学习率，即实际缩放 $\alpha' = \frac{\alpha}{r}$）。

**训练流程**：只优化 $A$ 和 $B$；推理时若想消除 LoRA 开销，可合并 $W' = W_0 + \frac{\alpha}{r} BA$，此时**推理零额外延迟**——这是 LoRA 比 adapter（多一层串行模块）工程上更优的核心原因。

### 4.3 为什么低秩足够：参数与显存账本

**参数占比**：以 7B 模型、$r=16$、对 q/k/v/o 和两块 MLP 加 LoRA 为例——每层 LLM 维度 $d=4096$，$d \times r + r \times d = 2 \times 4096 \times 16 \approx 131K$ 个新参数/层，而原层有 $4 \times 4096^2 \approx 67M$ 个参数，**LoRA 新增参数约占模型的 0.1%–1%**（具体取决于加在哪些模块）。

**显存收益**（关键账本）：

$$\text{显存} \approx \underbrace{\text{模型权重}}_{\text{固定}} + \underbrace{\text{梯度}}_{\text{仅可训练}} + \underbrace{2 \times \text{可训练参数}}_{\text{Adam 两阶矩}} + \underbrace{\text{激活}}_{\text{与 batch/seq 相关}}$$

全参微调时"梯度 + 优化器状态"是模型参数的好几倍；LoRA 时这两项只有几 MB～几十 MB，**显存大头退回为基座权重本身（BF16 下 7B ≈ 14 GB）和激活**，因此 7B 用 LoRA 在 24 GB 单卡可行，QLoRA 甚至可在 16 GB 训练。

### 4.4 超参选择

| 超参 | 常用范围 | 影响 | 建议 |
|------|---------|------|------|
| rank $r$ | 8–64 | 越高表达能力越强，越容易过拟合 | 指令微调 $r=16$ 起步，64 很少带来显著提升 |
| $\alpha$ | 等于或 2 倍于 $r$ | 控制更新幅度 | $\alpha=16$ 配 $r=16$（即缩放 1）常见 |
| dropout | 0–0.1 | 防过拟合 | 数据少时 0.05–0.1 |
| target modules | q/k/v/o、MLP | 决定哪些层获得低秩更新 | 默认全加；只加 q/v 效果略降但更省 |
| 训练层 | 全部 transformer 层 vs 只最后几层 | 最后几层偏向行为、全层偏向能力 | 指令微调建议全层，避免只改输出层 |
| 偏置 | none / all / lora_only | 是否也训 bias | 默认 none 即可 |

**经验结论**：LLM 各层对 LoRA 的敏感度不同——attention 的 q 矩阵（尤其是靠近输出层的）对"指令遵循"影响最大；MLP 层对知识性内容的保持重要。VLM 微调中一个常见做法是**LLM 部分全上 LoRA，视觉塔不动**。

### 4.5 常见误用与反例

1. **rank 越大越好？** 不是。$r=256$ 在指令微调上常不如 $r=16$——低秩约束本身就是正则化；
2. **LoRA 一定能达到全参效果？** 大规模预训练数据上差距明显；但 SFT 场景（数据少、epoch 少）差距很小，甚至偶尔更好（正则效应）；
3. **合并后效果变化？** 合并 $W_0 + \Delta W$ 在数学上精确等价（没有近似损失），这是它优于 prefix-tuning 等方法的工程优势。

### 4.6 QLoRA：4-bit 量化 + LoRA

QLoRA（Dettmers et al., 2023）把"LoRA 训练时的基座"进一步压缩为 **4-bit 量化模型**：

| 组件 | 说明 |
|------|------|
| NF4 量化 | NormalFloat4，按信息论最优分位点切分数值范围，对零均值高斯分布的数据比均匀量化误差小 |
| 双重量化 | 对量化常数再量化一次，进一步省显存 |
| 分页优化器 | 显存不够时把优化器状态换页到 CPU RAM，自动管理 |
| LoRA | 在量化基座上以 BF16 训练低秩增量 |

量化带来的精度损失由 LoRA 的 BF16 训练部分补偿，最终效果：**7B 模型可在 8 GB 显存训练，效果接近 BF16 LoRA**（论文报告从 16-bit 到 4-bit 在多项任务上 < 1% 差异）。

**面试要点**：QLoRA 的"4-bit"指**基座权重**的存储精度，**LoRA 增量参数和优化器状态仍是 16-bit/32-bit**——训练精度不低，这也是它效果不崩的根本原因。

---

## 五、多模态微调的特有考量（重点）

### 5.1 视觉塔：冻结还是微调？

| 策略 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| 冻结视觉塔 | 省显存（不存 ViT 梯度）；避免破坏预训练对齐；训练快 | 视觉特征与任务不匹配时无法自适应 | 通用域 VLM 微调（默认选择） |
| 微调视觉塔（全参或 LoRA） | 高分辨率、域特殊图（医学、遥感）下效果明显提升 | 显存增加；可能破坏 CLIP 预训练对齐导致幻觉 | 域差异大的场景、图像质量要求高的任务 |
| 只微调最后几层 ViT | 折中 | 调参复杂 | 中等域差异 |

关键权衡点——**对齐破坏风险**：视觉塔（CLIP/SigLIP）的语言侧与 LLM 的词嵌入是两个不同的语义空间。微调视觉塔会让图像特征分布偏移，而投影层和 LLM 是为"旧的分布"训练的，三者一起动容易互相拉扯，收敛不稳定、幻觉变多。

**什么时候必须微调视觉塔**：

1. 输入分辨率远高于预训练分辨率（如 ViT 预训练 224，任务用 1024），位置编码与 patch 分布的分布漂移需要视觉塔适应；
2. 域数据极其特殊（病理切片、卫星图、X 光），CLIP 学过的自然图像分布完全失效；
3. 训练数据量大且质量高，足以支撑视觉塔的重对齐。

**工程折中**：对视觉塔用 LoRA（比全参省显存）或用 1/10 学习率（比 LLM 低一个量级），把"对齐漂移"控制在可接受范围。

### 5.2 投影层：训练策略

| 方案 | 含义 | 典型阶段 |
|------|------|---------|
| 冻结 | 投影层完全不动 | 极少见，除非投影层已充分训练 |
| 与 LLM 同 lr | 一起更新 | Stage-2 默认 |
| 独立高 lr | 视觉侧梯度信号弱，给投影层更高学习率补偿 | 部分工作（如视觉侧加 lr multiplier 2–10 倍） |

投影层参数量极小（几百万），无论训不训都几乎不影响显存；但**它决定了视觉信息能否以 LLM 期望的分布进入语言侧**，是 VLM 微调中最常被忽视的"连接器"。

### 5.3 数据配比：纯文本 vs 图文数据

VLM 微调常出现**纯文本能力退化**（灾难性遗忘）：长时间只喂图文数据，模型在纯文本推理、代码、通用对话上的能力下降。

| 配比 | 效果 |
|------|------|
| 100% 图文 | 视觉任务最优，但纯文本能力下降明显 |
| 图文 : 纯文本 ≈ 3:1 ~ 5:1 | 主流推荐，视觉任务损失小、文本能力保住 |
| 加入开源纯文本 SFT 数据 | 防止遗忘的关键手段（如加入 Alpaca、ShareGPT 文本子集） |

经验规律：**多模态 SFT 时保留 20%–30% 的纯文本指令数据**（与基座同分布），能显著缓解灾难性遗忘，代价是视觉指标轻微下降。

### 5.4 高分辨率与视觉 token 数量的显存影响

视觉 token 进入 LLM 后与文本 token 一起过 attention，**显存和计算随序列长度平方增长**：

$$\text{attention 复杂度} = O\left((N_{\text{img}} + N_{\text{text}})^2\right)$$

| 分辨率设置 | patch 数（patch=14） | 视觉 token 数 | 影响 |
|-----------|---------------------|--------------|------|
| 336×336 | 24×24 = 576 | 576 | 标准 LLaVA-1.5 配置 |
| 672×672 | 48×48 = 2304 | 2304 | 显存约翻 3–4 倍 |
| 多塔融合（如 2×336） | 2×576 = 1152 | 1152 | 显存约翻 2 倍 |

训练实操影响：**batch size 必须随分辨率降低**（同显存下 672 分辨率 batch 往往只能开到 336 的 1/4）；7B 模型 + 672 分辨率 + 长回答时，24 GB 单卡可能只能跑 batch=1 + 梯度累积。这也是为什么 VLM 微调里"分辨率、序列长度、batch size、LoRA 设置"必须一起联调。

### 5.5 多模态 SFT 常见失败模式

| 失败模式 | 表现 | 根因 | 对策 |
|---------|------|------|------|
| 过拟合到图像描述 | 无论问什么都输出一长段环境描述 | 数据里"详细描述"类占比过高 | 平衡数据类型，增加问答/推理类 |
| 忽视指令（ignoring instruction） | 答非所问、输出与图像无关的通用话术 | 指令多样性与 prompt 质量不足 | 扩充指令改写、负样本、指令跟随类数据 |
| 幻觉（hallucination） | 编造图中不存在的物体/属性 | 训练数据中存在"图答不符"的样本；微调视觉塔破坏对齐 | 数据质检、负样本、CPO 等幻觉消减训练 |
| 模板套话 | 所有回答以固定句式开头（"好的！根据图片……"） | 训练数据模板单一、epoch 过多 | 去重模板、减少 epoch、增加模板多样性 |
| 纯文本能力塌方 | 代码/数学能力明显下降 | 图文数据挤压文本数据 | 配比中加入纯文本 SFT 数据 |
| 多轮退化 | 单轮好、多轮差（重复提问、丢失上文） | 数据多轮占比太低 | 构造 30%+ 多轮数据 |

---

## 六、指令数据构造（重点）

### 6.1 数据来源

| 来源 | 代表 | 优点 | 缺点 |
|------|------|------|------|
| 公开 VLM 指令数据 | LLaVA-158K、ShareGPT4V、M3IT、Otter、LRV-Instruction | 现成、覆盖广 | 与任务域不匹配、有噪声 |
| 人工标注 | 众包、内部标注平台 | 质量最高、与业务对齐 | 贵、慢、量小 |
| 大模型生成 | GPT-4V 看图出题、改写扩写（LLaVA 的官方路线） | 成本低、规模大、风格可控 | 有幻觉与偏见、需过滤 |
| 从已有标注反推 | 用现有 VQA/caption 数据集的标注 + 模板组装 | 便宜 | 模板单一、多样性差 |
| 业务日志 | 线上真实请求 | 与真实分布最匹配 | 隐私、规模、长尾质量 |

> **面试记忆点**：LLaVA 的 158K 是怎么来的——用 COCO 图像 + 3 个来源的文本标注（coco caption、region-level captions、bbox 标注），喂给 **GPT-4** 生成 4 类指令数据（对话、详细描述、复杂推理）。**"大模型造数据"是 2023 年后数据生产的主流范式。**

### 6.2 数据类型全景

| 类型 | 数据内容 | 考察能力 | 示例问题 |
|------|---------|---------|---------|
| 单轮 QA | 一图一问一答 | 基础理解 | "图里有什么？" |
| 多轮对话 | 多轮追问、引用上文 | 上下文建模、指代消解 | "第二张图呢？" |
| 详细描述 | 长文本、结构化叙述 | 全局感知、语言组织 | "详细描述整幅图" |
| 复杂推理 | 需要逻辑链、常识、计数 | 组合推理、常识 | "如果下雨，图中的聚会怎么办？" |
| OCR | 识别文字 | 文本感知 | "招牌上的字是什么？" |
| 图表理解 | 柱状图、折线图、表格 | 数字提取、趋势判断 | "哪个月增长最快？" |
| Grounding / 指代定位 | 回答带坐标/框/区域引用 | 空间定位 | "猫在哪里？（输出 bbox）" |
| 拒绝回答 | 图像无信息、问题越界 | 对齐与边界 | "图上没有相关信息，无法回答。" |

### 6.3 数据质量要素（比数量更重要的四个维度）

| 维度 | 具体操作 | 为什么要做 |
|------|---------|-----------|
| 难度分布 | 简单描述 : 中等推理 : 高难推理 ≈ 5:3:2 | 全简单模型不涨能力；全难模型训练不稳、输出崩坏 |
| 多样性 | 视觉多样性（场景/物体/风格）+ 语言多样性（提问句式、回答风格） | 防止过拟合模板、泛化到长尾指令 |
| 指令多样性 | 对同一条 QA 做指令改写（"回答""请问""帮忙分析"……多种问法） | 模型对同义指令的鲁棒性；避免"指令匹配记忆" |
| 负样本 / 拒绝回答 | 无答案问题、"不要看图直接推理"型样本、误导性提问 | 学会边界行为，减少幻觉与过度自信 |

### 6.4 数据清洗流水线

```text
原始数据
  → 1. 格式校验（JSON 合法性、role 枚举、图像路径存在性）
  → 2. 启发式过滤（回答长度 < 5 字符、重复模板前缀、全角/乱码、HTML 残留）
  → 3. 去重（文本相似度 / MinHash 近似去重，指令与回答都要查）
  → 4. 质量打分（GPT-4 / 小模型打分：相关性、事实性、完整性，卡阈值）
  → 5. 配比与采样（按类型/难度/来源做分层采样，控制长尾）
  → 6. 验证集划分（按指令 hash 分层抽 1%–5%，确保与训练不重复）
```

关键点：**清洗的目标不是"删得干净"，而是"分布可控"**——你要知道最终数据里每一类占多少，而不是被原始数据的天然分布牵着走。

### 6.5 模板示例（JSON 示意）

单轮 QA（含图像占位符）：

```json
{
  "id": "sample_0001",
  "image": "train/0001.jpg",
  "conversations": [
    {
      "role": "user",
      "content": "<image>\n图片里的人在做什么运动？"
    },
    {
      "role": "assistant",
      "content": "他在打网球，右手正挥拍击球，动作处于发力阶段。"
    }
  ]
}
```

多轮对话（system 可选）：

```json
{
  "id": "sample_0002",
  "image": "train/0002.png",
  "conversations": [
    {
      "role": "user",
      "content": "<image>\n这是一张什么图表？"
    },
    {
      "role": "assistant",
      "content": "这是一张 2024 年某公司各季度营收的柱状图。"
    },
    {
      "role": "user",
      "content": "哪一季度增长最快？增长率大约是多少？"
    },
    {
      "role": "assistant",
      "content": "第三季度增长最快，环比增长率约为 32%。"
    }
  ]
}
```

带目标框的 Grounding 数据（bbox 坐标归一化到 [0,1]）：

```json
{
  "id": "sample_0003",
  "image": "train/0003.jpg",
  "conversations": [
    {
      "role": "user",
      "content": "<image>\n请定位图中所有红色车辆的位置。"
    },
    {
      "role": "assistant",
      "content": "红色车辆共有 2 辆：第一辆位于 [0.12, 0.35, 0.28, 0.52]，第二辆位于 [0.60, 0.10, 0.75, 0.30]。"
    }
  ]
}
```

### 6.6 数据配比实验结论：质量 > 数量

| 工作 | 数据量 | 结论 |
|------|--------|------|
| LIMA | 1000 条高质量指令 | "超大模型 + 极少但极高质量数据"即可对齐：质量是最关键因子 |
| LLaVA-158K | 15.8 万条 GPT-4 生成 | 覆盖面广（对话/描述/推理三类）足以支撑 SOTA 视觉对话 |
| Alpaca | 52K | 模板化生成即可获得基础指令遵循能力，但多样性不足时上限低 |
| 多数据源拼接 | 百万级混合 | 简单堆量收益递减，**配比与清洗比总量更重要** |

三个实践结论（可直接用于面试回答）：

1. **质量 > 数量**：1 万条干净数据经常胜过 10 万条噪声数据；LIMA 的 1000 条结论是极端例证；
2. **多样性 > 总量**：同一模板重复 10 万次不如 1 万种问法；
3. **配比 > 随机**：类型比例（描述/推理/OCR/多轮）按任务目标分层设计，而不是"有什么用什么"。

---

## 七、DPO / RLHF 简述：对齐的下一步

SFT 解决"会按格式回答"，但**回答的价值观、偏好、安全性**需要更进一步的强化式对齐。

### 7.1 RLHF 的三阶段

| 阶段 | 内容 |
|------|------|
| 1. SFT | 上文的指令微调，得到基础模型 $\pi_{\text{SFT}}$ |
| 2. RM 训练 | 用人类对同一输入的两个回答做偏好标注，训练奖励模型 $r_\phi$：$\mathcal{L}_{\text{RM}} = -\log \sigma(r_\phi(x, y_w) - r_\phi(x, y_l))$ |
| 3. PPO | 以 RM 为奖励，用 PPO 强化 $\pi_{\text{SFT}}$，并加 KL 约束防止偏离过远 |

### 7.2 DPO：绕过 RM 和 PPO

DPO（Direct Preference Optimization, 2023）的核心观察：**RLHF 的最优策略有闭式解**，可以把偏好直接转化为隐式奖励：

$$\mathcal{L}_{\text{DPO}} = -\log \sigma\left( \beta \log \frac{\pi_\theta(y_w \mid x)}{\pi_{\text{ref}}(y_w \mid x)} - \beta \log \frac{\pi_\theta(y_l \mid x)}{\pi_{\text{ref}}(y_l \mid x)} \right)$$

直觉：

- 分子分母的比值 $\log \frac{\pi_\theta(y \mid x)}{\pi_{\text{ref}}(y \mid x)}$ 衡量"新模型相对参考模型（SFT 模型）在某回答上的提升幅度"；
- 目标：**让偏好回答（$y_w$）的相对提升大于非偏好回答（$y_l$）的相对提升**，两者差值过 sigmoid + 交叉熵，就是一个标准二分类损失；
- $\beta$：控制与参考模型的 KL 距离，越大越保守。

**与 SFT 的关系**：DPO 的参考模型就是 SFT 模型，训练数据同样来自偏好对（由 SFT 模型采样生成候选回答 + 人类标注偏好）。VLM 领域已有 DPO 变体（如对幻觉数据的负偏好）用于**幻觉消减**，是"SFT 之后还能做什么"的标准答案。

---

## 八、微调实验方法论

### 8.1 实验设计：base vs SFT vs LoRA

| 对比组 | 目的 |
|--------|------|
| Base 基座（不微调，零样本） | 基线：确认"不微调差在哪里"，评估微调的边际收益 |
| 全参 SFT（算力允许时） | 效果上限参考，判断 LoRA 的差距是否可接受 |
| LoRA（不同 rank/α） | 主方案调优：rank 16 vs 64、加不加视觉塔 |
| QLoRA | 显存受限时的降级方案对比 |
| 不同数据配比 | 验证"质量/多样性/类型比例"假设 |

**变量控制原则**：一次只动一个变量。比如比较 LoRA 与全参微调时，数据、epoch、lr schedule、seed 必须完全一致，否则结论不可信。

### 8.2 评测集划分与指标

| 类别 | 指标 | 说明 |
|------|------|------|
| 训练/验证/测试 | 按指令 hash 分层抽样，验证与训练无数据重叠 | 防止数据泄漏导致虚高 |
| 任务指标 | VQA 类 acc、Caption 类 CIDEr/BLEU、OCR 类 CER、Grounding 类 IoU | 按任务分别上报 |
| 行为指标 | 拒绝率、格式正确率（JSON 解析成功比例）、多轮一致性 | SFT 特有的"行为质量"检查 |
| 资源指标 | 峰值显存、每 step 耗时、总训练时长 | 支撑方案选型 |
| 稳定性 | 不同 seed 跑 2–3 次看方差 | 防止一次实验结果偶然 |

### 8.3 失败案例分析框架

```text
失败现象 → 定位层面（数据 / 训练 / 评估）→ 假设 → 小实验验证 → 结论沉淀
```

| 现象 | 优先怀疑对象 | 验证方式 |
|------|-------------|---------|
| 训练 loss 不降 | 数据格式（labels 全 -100 是头号 bug）、lr 过大/过小 | 抽一条数据可视化 labels 与 loss 区间 |
| 输出全是模板 | epoch 过多、数据模板单一 | 看验证集多样指令的表现 |
| 幻觉严重 | 数据存在"图答不符"、微调了视觉塔 | 抽查 100 条训练数据人工过一眼 |
| 纯文本能力下降 | 数据配比失衡 | 单独跑纯文本评测集 |
| 高分辨率输入崩坏 | 位置编码/resize 处理、显存不足导致截断 | 对比不同分辨率同一条指令的输出 |

---

## 九、高频面试问答

**Q1：为什么 SFT 只对回答部分计算 loss？**
三个原因：防止模型记忆问题与"预测下一个问题"的无意义能力；训练信号集中在回答部分，效率高；保持对指令的敏感边界。实现上就是 label mask（-100 置为 ignore index）。

**Q2：LoRA 为什么有效？**
核心假设是微调导致的权重增量 $\Delta W$ 低秩。微调是在强预训练点上做小幅行为调整，所需自由度远小于参数总量；SVD 实验证实 $\Delta W$ 前几个奇异值就占主导。因此用 $W + \frac{\alpha}{r}BA$ 近似即可，参数少、显存省、可合并推理零开销。

**Q3：LoRA 和全参微调的效果差距有多大？**
SFT 场景（数据量小、1–3 epoch）差距很小，LoRA 甚至因低秩正则偶有优势；但大规模数据、深度能力学习场景差距明显。QLoRA 论文报告 4-bit 基座 + LoRA 与 16-bit 全参在多数任务上差距 < 1%。结论要分场景，不要一概而论。

**Q4：视觉塔该不该冻结？**
默认冻结（省显存、保护对齐）；域差异大（医学、遥感）、分辨率漂移大、数据量足时微调，优先用 LoRA 或低学习率折中。核心风险是微调视觉塔破坏 CLIP 预训练对齐导致幻觉。

**Q5：如何构造指令数据？**
来源（公开数据 / 人工标注 / 大模型生成）→ 类型配比（描述/推理/OCR/多轮）→ 质量四要素（难度、多样性、指令多样性、负样本）→ 清洗流水线（去重、过滤、打分）→ 分层划分验证集。核心结论：质量 > 数量、多样性 > 总量、配比 > 随机。

**Q6：多模态微调时纯文本数据占多少？**
一般 20%–30%。目的：防止灾难性遗忘，保住 LLM 的代码、数学、通用对话能力。全图文数据会让文本能力明显退化。

**Q7：QLoRA 的 4-bit 量化为什么损失很小？**
量化的是基座权重（NF4 按信息论最优分位点切分，误差小），LoRA 增量与优化器状态仍是 16/32-bit，训练精度不打折；另有双重量化与分页优化器兜底显存。4-bit 基座 + BF16 增量 = 效果接近全精度。

**Q8：LoRA 的 rank 和 alpha 怎么定？**
rank 8–64，指令微调常用 16；alpha 取等于或 2 倍 rank；两者协同控制更新幅度，实践上更多靠 $\frac{\alpha}{r}$ 整体缩放调学习率。rank 不是越大越好，低秩约束本身是正则。

**Q9：SFT 和 DPO 是什么关系？**
SFT 是监督行为迁移（学格式与任务），DPO 是偏好对齐（学好坏偏好）。DPO 以 SFT 模型为参考模型，用偏好对直接优化，不需要 RM 和 PPO，是 SFT 之后的下一步。VLM 里 DPO 常用于幻觉消减。

**Q10：微调后模型知识变多了吗？**
基本不变。知识在预训练已沉淀，SFT 改变的是输出条件分布 $P(\text{answer} \mid I, \text{instruction})$ 的形状（格式、任务行为、边界）。这也是为什么"微调解决不了基座不会的知识"——没见过的事物，怎么微调都答不出来。

---

## 十、常见误区

**误区 1：SFT 就是让模型再学一遍知识，数据越多越好。**
错。SFT 是行为迁移，数据质量与配比远重要于数量；LIMA 的 1000 条实验与 LLaVA 的 158K 设计都说明这一点。堆量且不洗数据反而引入噪声。

**误区 2：LoRA 一定比全参微调差。**
不完全对。指令微调场景差距很小甚至持平（低秩正则效应）；但大规模数据或深度能力学习时差距明显。结论依赖场景。

**误区 3：LoRA 的 rank 越大效果越好。**
错。rank 过大失去正则作用且易过拟合，$r=256$ 常劣于 $r=16$。低秩约束本身是正则化，这也是 LoRA 有效性的一个来源。

**误区 4：QLoRA 的"4-bit"意味着训练精度很低。**
错。4-bit 只指基座权重的存储格式；LoRA 增量、梯度、优化器状态仍是高精度。训练精度并不低，因此效果才能接近全精度微调。

**误区 5：微调视觉塔一定提升 VLM 效果。**
错。多数情况下冻结视觉塔更好——微调视觉塔破坏预训练图文对齐，且需要高质量大数据的支撑，否则幻觉变多、训练不稳。域差异大时才值得动，且优先 LoRA/低 lr。

**误区 6：SFT 训练集 loss 降到很低就是成功。**
错。指令微调极易过拟合，训练 loss 逼近 0 往往是"背诵数据"的信号；判断标准永远是验证集与人工抽测，而不是训练 loss。

---

## 十一、自我检验

- [ ] 能说清"预训练 vs SFT"的能力分工（语义映射 vs 行为规范）
- [ ] 能写出 SFT 的带 mask 交叉熵公式并解释每个符号
- [ ] 能说清为什么只对回答算 loss（三个理由）
- [ ] 能默写 LoRA 的 $h = W_0x + \frac{\alpha}{r}BAx$ 并解释 A/B 初始化
- [ ] 能讲明白"为什么微调增量是低秩的"（含 SVD 证据）
- [ ] 能算一笔 LoRA 的显存账（优化器状态从 56 GB 降到几 MB 的账本）
- [ ] 能说清 QLoRA 的 NF4、双重量化、分页优化器三件套
- [ ] 能对"视觉塔冻结 vs 微调"给出分场景结论
- [ ] 能说清高分辨率 → 视觉 token → 显存平方增长的因果链
- [ ] 能列举多模态 SFT 的 5 种以上失败模式及对策
- [ ] 能说出 4 种数据来源与各自的优缺点
- [ ] 能说出 4 个质量要素与"质量 > 数量"的论文证据
- [ ] 能写出 2 种数据 JSON 模板（单轮/多轮）
- [ ] 能讲出 DPO 损失直觉（偏好回答相对提升 > 非偏好回答）
- [ ] 能设计 base vs SFT vs LoRA 的对比实验并控制变量
- [ ] 能回答 10 个高频面试问答题
- [ ] 能识别 6 个常见误区

---

## 参考文献

1. [LoRA: Low-Rank Adaptation of Large Language Models](https://arxiv.org/abs/2106.09685) — Hu et al., ICLR 2022
2. [QLoRA: Efficient Finetuning of Quantized LLMs](https://arxiv.org/abs/2305.14314) — Dettmers et al., NeurIPS 2023
3. [Visual Instruction Tuning (LLaVA)](https://arxiv.org/abs/2304.08485) — Liu et al., NeurIPS 2023
4. [Improved Baselines with Visual Instruction Tuning (LLaVA-1.5)](https://arxiv.org/abs/2310.03744) — Liu et al., 2023
5. [LIMA: Less Is More for Alignment](https://arxiv.org/abs/2305.11206) — Zhou et al., NeurIPS 2023
6. [Direct Preference Optimization: Your Language Model is Secretly a Reward Model](https://arxiv.org/abs/2305.18290) — Rafailov et al., NeurIPS 2023
7. [ShareGPT4V: Improving Large Multi-Modal Models with Better Captions](https://arxiv.org/abs/2311.12793) — Chen et al., ECCV 2024
